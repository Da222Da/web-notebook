{
  "name": "web",
  "version": "1.0.0",
  "description": "基于 JavaScript 的 Web 应用开发",
  "main": "index.js",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=godot-docs",
    "build": "npm run build --workspace=godot-docs",
    "deploy": "git add . & git commit -m 'deploy' & git push",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
